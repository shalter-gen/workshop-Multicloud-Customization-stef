name: Update body color in theme-mine.css
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Make Body Font Color Black
        uses: jacobtomlinson/gha-find-replace@2.0.0
        with:
          #find: "--MAIN-TEXT-color:#323235;"
          #replace: "--MAIN-TEXT-color:#000000;"
          find: "--MAIN-TEXT-color:#000000;"
          replace: "--MAIN-TEXT-color:#000001;"
          #include: "**.css"
          include: "static/css/theme-mine.css"
          regex: false
      - name: Commit & Push changes
        uses: actions-js/push@master
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          message: GHA Auto-Update Template for GTS

      # - name: Push changes
      #   uses: ad-m/github-push-action@v0.6.0
      #   with:
      #     github_token: ${{ secrets.GITHUB_TOKEN }}
      #     branch: ${{ github.ref }}
